<div class="row">
  <div class="col col-md-6 col-md-offset-3">
<%= form_with(model: kotoba, local: true) do |form| %>
  <% if kotoba.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kotoba.errors.count, "error") %> prohibited this kotoba from being saved:</h2>

      <ul>
        <% kotoba.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :word %>
    <%= form.text_field :word, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_field :message, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "登録", class: "btn btn-primary" %>
  </div>
<% end %>
  </div>
</div>
